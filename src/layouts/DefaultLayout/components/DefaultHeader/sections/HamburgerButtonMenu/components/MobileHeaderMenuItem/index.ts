import MobileHeaderMenuItem from './MobileHeaderMenuItem';
import { motion } from 'framer-motion';

export const MMobileHeaderMenuItem = motion(MobileHeaderMenuItem);
export default MobileHeaderMenuItem;
